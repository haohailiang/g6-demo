/**
 * 功能: 图谱常量
 * 作者: 郝海亮
 * 日期: 2020-11-14
 */

//  布局
const nodeWidth = 40
const nodeHeight = 72
const hGap = 7

// 背景框
const portraitX = 20
const portraitY = 20
const portraitRadius = 20

// icon
const portraitIconX = portraitRadius * (1 - 5 / 9)
const portraitIconY = portraitRadius * (1 - 5 / 9)
const portraitIconRadius = portraitRadius * 5 / 9

// image
const portraitImageX = 0
const portraitImageY = 0
const portraitImageRadius = portraitRadius

// 统计数字
const statX = 20
const statY = 20
const statFontSize = 14

// 姓名
const nameX = 20
const nameY = 48
const nameFontSize = 14

// 职位
const positionX = 20
const positionY = 62
const positionFontSize = 12

// 加号
const markPlusX = 20
const markPlusY = 100
const markPlusRadius = 5
const markPlusLineWidth = 2
