export default class CustomComboLayout {
    constructor(options) {
        this.options = options;
    }

    layout(combo) {
        console.log('combo: ', combo);
        // 在此处实现您自己的布局计算逻辑
        // 例如，根据 combo 的大小、元素数量等决定元素的位置
    }
}
