// const minimap = new G6.Minimap({
//     size: [150, 100],
// });

const options = {
    modes: {
        default: ['zoom-canvas', 'drag-canvas', 'drag-node'],
    },
    animate: true,
    // plugins: [minimap],
}

export default options
